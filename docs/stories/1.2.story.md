# Story 1.2 — Dashboard DONO com Métricas Reais

**Epic:** E1 — Fundação do Dono
**Status:** InProgress
**Agente:** @dev (Dex)
**Branch:** master

---

## Objetivo

O dashboard do DONO em `/dono/inicio` possui dados mock fixados no código (gráfico semanal, "Reativar clientes: 3"). Esta story conecta tudo a dados reais da API.

## Acceptance Criteria

- [ ] AC-1: Criar `GET /api/dono/metrics` que retorna: agendamentos de hoje, receita do mês, em atendimento, clientes inativos (30+ dias)
- [ ] AC-2: Gráfico semanal com dados reais de agendamentos dos últimos 7 dias
- [ ] AC-3: Card "Reativar clientes" com contagem real de pets sem visita há 30+ dias
- [ ] AC-4: Automação de Retorno exibe pet/cliente real (não mock)
- [ ] AC-5: Loading skeleton enquanto carrega
- [ ] AC-6: Estado vazio quando não há agendamentos

## Tasks

- [ ] T1: Criar `GET /api/dono/metrics`
- [ ] T2: Criar `GET /api/dono/weekly-chart` (agendamentos por dia últimos 7 dias)
- [ ] T3: Criar `GET /api/dono/inactive-clients` (pets sem visita 30+ dias)
- [ ] T4: Atualizar `app/dono/inicio/page.tsx` para consumir APIs reais
- [ ] T5: Remover todos os mocks fixados no código

## File List

- `app/api/dono/metrics/route.ts` — novo
- `app/api/dono/weekly-chart/route.ts` — novo
- `app/api/dono/inactive-clients/route.ts` — novo
- `app/dono/inicio/page.tsx` — atualizar
